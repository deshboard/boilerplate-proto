syntax = "proto3";

package boilerplate;

message Request {}

message Response {}

service Boilerplate {
    rpc Method (Request) returns (Response);

    rpc StreamingMethod (Request) returns (stream Response);
}
